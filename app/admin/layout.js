export const metadata = {
  title: 'Admin — AMSC Performance',
  robots: { index: false, follow: false }, // Don't index admin page
};

export default function AdminLayout({ children }) {
  return children;
}
